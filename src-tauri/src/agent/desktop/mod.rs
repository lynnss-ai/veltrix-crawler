//! 🖱️ 桌面操作工具模块(独立、可复用)。
//! 工具集:截屏 / 列窗口 / 剪贴板读写 / 鼠标(移动·点击·滚动·拖拽)/ 键盘(输入·组合键)/
//! 窗口控制(focus·最大化·最小化·关闭,Windows)/ 启动程序 / 打开文件或 URL。
//! 不绑定具体 Agent、不绑前端;经 `tools::build_registry(app)` 拿到注册表后挂到任意 ReAct 循环。

pub mod tools;
